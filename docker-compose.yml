services:
  collectarr:
    build: .
    ports:
      - "8893:8893"
    volumes:
      - ./data:/data
      - /data/media:/media
      - ./frontend/build:/app/frontend/build:ro
    environment:
      - MEDIA_PATH=/media
      - DB_PATH=/data/collectarr.db
      - PORT=8893
