services:
  collectarr:
    build: .
    ports:
      - "3000:8080"
    volumes:
      - ./data:/data
      - ${MEDIA_HOST_PATH}:/media
      - ./frontend/build:/app/frontend/build:ro
    environment:
      - MEDIA_PATH=/media
      - DB_PATH=/data/collectarr.db
      - PORT=8080
